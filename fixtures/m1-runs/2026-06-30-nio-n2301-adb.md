# 2026-06-30 NIO N2301 ADB M1 Smoke

date: 2026-06-30
device slot: D
manufacturer/model: NIO N2301
android version/api: Android 14 / API 34
build channel: local debug APK
transport: ADB forward to debug harness Activity endpoint
handshake attempts: 1/1 passed after foreground debug harness launch
visible time: device already authorized over USB
first list time: not measured
100MB download: not run
100MB upload: not implemented
resume result: covered by local unit test, not exercised on device in this run
permission cases: media image listing available after permissions were granted
diagnostics bundle: `m1-smoke` returned diagnostics errors=0
notes:

- Starting only `ForegroundConnectionService` through `adb shell am start-foreground-service` created the listener, but the NIO device froze the background process. ADB forward connections reached the socket queue and timed out because Java `accept()` did not run.
- Starting `DebugHarnessActivity` with `--ei port 39401` kept the app foreground and unfrozen.
- `m1-smoke` passed on local forwarded port `64142`: server `DroidMatchAndroid`, device `NIO N2301`, SDK `34`, battery `93`, roots `3`, diagnostics errors `0`.
- `list-dir --path dm://roots/` returned Images, Videos, and App Sandbox.
- `list-dir --path dm://media-images/` returned 47 entries.
- `download --source-path dm://media-images/media/1000001370 --chunk-size 65536` downloaded 127695 bytes in 2 chunks to `/tmp/droidmatch-nio-screenshot.jpg`.
- Download SHA-256: `cfe0b2d7905f83bf83872126b262575604ff77753851a8e0925adb291bf7a0e0`.
