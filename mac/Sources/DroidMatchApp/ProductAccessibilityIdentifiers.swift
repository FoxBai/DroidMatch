enum ProductAccessibilityIdentifiers {
    // Shared with the repository AX probe at compile time. Keep this stable so
    // attended product evidence never falls back to matching arbitrary UI text.
    static let discoveryDeviceCard = "app.droidmatch.discovery-device-card"
}
