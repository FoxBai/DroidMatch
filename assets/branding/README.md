# DroidMatch Temporary Icon

`droidmatch-temp-icon.png` is a temporary GPT-generated app icon source image for M1 development builds.

Prompt summary:

```text
Temporary app icon for a macOS + Android device manager named DroidMatch. No text or letters. Show a generic phone and desktop/laptop panel connected by a luminous USB/data bridge forming a subtle checkmark. Avoid official Android robot and Apple logo. Modern macOS/iOS-style app icon, graphite base, teal/cyan connection glow, green accent, small warm coral highlight.
```

The Android launcher PNGs under `android/app/src/main/res/mipmap-*` are resized derivatives of this source image.
